services:
  app:
    build: .
    ports:
      - "3000:3000"
    volumes:
      - ./data:/app/data
      - ./uploads:/app/uploads
    environment:
      - DB_PATH=/app/data/logs.db
      - PORT=3000
    restart: unless-stopped
