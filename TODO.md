# List of TODOs

- Create a small menu that allows me to pick either the upload page or the Dashboard page
- Add a new filter to only show specific request types on the Dashboard (GET, POST, PUT, PATCH, ...)
- Evaluate changing the database from SQLLite to MongoDB or PostgreSQL for better performance
- Add a "clear all data" button on the upload page that wipes all imports and log entries from the database
- In the Top 10 URL Graphic add a better tooltip that allows the full URL to be read

# Rules

When implementing any of the TODO's above make sure to follow this step-by-step:

1. Evalute all TODO's and pick which one to implement next.
2. Confirm with me if I agree with the pick and if not go back to step 1
3. Implement the changes asking me to validate them each time
4. Update the PLAN.md to add or change the planning information
5. Update the SPECIFICATION.md to add or change the specification of the project
6. Remove the TODO from this file.
7. Suggest me a new TODO to add to the list.
8. Give me a commit message with the changes made
